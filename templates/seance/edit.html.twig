{% extends 'base.html.twig' %}

{% block title %}Editer une séance{% endblock %}

{% block body %}
    <h1>Editer une séance</h1>

    {{ include('seance/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('seance_index') }}">Retour à la liste des séances</a>

    {{ include('seance/_delete_form.html.twig') }}
{% endblock %}
