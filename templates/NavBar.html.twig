<nav>
    <div class="logo">
        <img src="{{ asset('img/logo.png') }}" alt="logo">
        <span>FOX Theater</span>
    </div>
    <div class="menu">
        <div id="marker"></div>
        <a href="{{ path('accueil') }}">Accueil</a>
        <a href="{{ path('seance_index') }}">Séances</a>
        <a href="{{ path('salle_index') }}">Salles</a>
        <a href="{{ path('show_films') }}">Disponibilité</a>
    </div>
</nav>