{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle numero {{ salle.id }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero de la salle</th>
                <td>{{ salle.numero }}</td>
            </tr>
            <tr>
                <th>Nombre de places</th>
                <td>{{ salle.nbPlaces }}</td>
            </tr>
            <tr>
                <th>Seance</th>
                <td>{{ seance.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salle_index') }}">Retour à la liste</a>

    <a href="{{ path('salle_edit', {'id': salle.id}) }}">Editer</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
